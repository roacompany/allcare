# 임신모드 v3 — 제로베이스 검토 판정 (2026-06-11)

8개 비판 렌즈(전제·수요·JTBD·차별화·실현가능성·제1원칙·결정재검증·반대명제) + 코드 사실검증 종합.

## 판정: (b) 축소/리프레임 — 강하게. 풀스코프 NO.
**8/8 렌즈가 "본격 추적기 9-서브프로젝트 풀빌드"를 거부.** 라이트 브릿지로 축소하거나, H-item 확보 불가 시 보류/접기.

## 코드 사실검증으로 무너진 진단 전제
- **P0 버그는 데이터모델 결함이 아니라 국소 결함**: 중복아기=멱등키 부재, 성별오류=3줄, 주수증가=archivedAt 미참조, 공유유실=ownerUserId 비영속. **전면 재작성 없이 국소 수정으로 잡힌다.**
- **v2 Pregnancy는 이미 정규화**(독립 컬렉션 + sharedWith + outcome enum + eddHistory + WriteBatch transitionState + 손실 case 코드 존재). 결함은 저장층이 아니라 **런타임 IA층(AppContext 4-state + 비영속 ownerUserId)**.
- ⚠️ 검토관이 "RC=0이라 노출 0"이라 했으나 — **내 직접 grep은 ContentView가 compile flag(true)를 읽음(RC 미배선)** → UI는 도달 가능. 단 버그 발현 경로 좁음(출산전환 재시도 등) + 한 번도 홍보·측정 안 됨. 어느 쪽이든 판정은 안 바뀜.

## 6개 결정 재판정
| 결정 | 판정 | 근거 |
|---|---|---|
| D6 만들 가치 | **REFRAME→사실상 REVERSE** | 8/8 풀빌드 거부. 라이트 브릿지 조건부 YES, 보류도 정직한 선택 |
| D1 본격 추적기 | **REVERSE** | 8/8 거부. 자기 리서치("가벼움=차별화") 정면 배신 |
| D2 데이터모델 새로 | **REFRAME** | 진단(프로필 엔티티) 방향은 맞되 전면교체+마이그레이션 철회. ownerUserId 영속화+IA층 교체로 P0 해소 |
| D3 대칭 공동편집 | **REVERSE/WEAKEN** | 8/8. 근거문서(비대칭 roles 맵) 뒤집음. 비대칭 복귀 시 충돌 표면 소멸 |
| D4 손실 v1 | **분열→수렴** | 기술안전장치(freeze·알림취소·보존·리뷰억제)=v1(코드만), 감수필요 콘텐츠(추모·임상카피)=분리 |
| D5 즉시 비활성 | **HOLDS(만장일치)** | 단 hotfix main 미머지, 재설계와 분리된 독립 위생작업 |

## 수렴 핵심 통찰
1. **진짜 블로커는 코드가 아니라 인간 게이트(H-items: OB/GYN 의료감수·법무·실기기 QA).** v1·v2를 죽인 벽. v3는 0개 해소하고 오히려 키움. 코드로 못 넘음.
2. **유일 방어가능 엣지 = "임신→육아 무손실 연속성"** (기존 사용자). 라이트 브릿지로 100% 확보. 광고0=위생, 검진/바우처=복제가능 콘텐츠격차.
3. **수요 한 번도 측정 안 됨.** "rollout 0%"=거부 아닌 미점화. 풀빌드=미검증 가설 선투자.
4. **DESIGN.md가 자기 근거 리서치를 2번 배신**(D3 대칭공유, D1 본격추적기).
5. **기회비용**: 명목 1순위 parking24(실매출 trigger 대기). 단일 PO·~20프로젝트. v2 "코드완성≠출시"를 더 큰 규모로 반복 위험.

## 가장 불편한 진실
1. 이 재설계는 시장신호가 아니라 의무감+기능선망에서 나옴. 우아한 데이터모델 ≠ 수요증거. 진단 전제조차 코드와 어긋남(국소수정으로 충분).
2. **가장 정직한 질문은 "9-서브프로젝트 짓자"가 아니라 "의사 섭외 가능한가?"** NO면 어떤 코드로도 못 켜고 매몰비용만 키움.
3. 광고0·수익0 상태에서 임신모드=순수 비용센터. retention 가치조차 미측정.

## PO 갈림길 3질문
- **Q1. OB/GYN + 변호사 실제 섭외 의지·채널 있나?** NO면 가장 정직한 답=임신모드 정식 보류+parking24 복귀.
- **Q2. 진짜 게임=신규 임산부 정복(→마미톡과 정면, 단일PO 난망) vs 기존 사용자 둘째임신 이탈방지(→라이트 브릿지면 충분)?** 후자면 기존 데이터로 "둘째 추가율×이탈율" 싸게 검증 가능.
- **Q3. parking24 매출 trigger 대기 중, 미측정 비용센터에 다개월 선투자 vs 라이트 브릿지 RC 5%로 전환율 숫자 먼저?**

전문: task output wj7hfavu5.
