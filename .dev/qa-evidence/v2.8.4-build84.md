# QA Evidence — v2.8.4 빌드 84 (Apple Health 정확 spec)

> deploy 체인 중 자동 bump 83→84. TestFlight Delivery UUID `9b2d5f7d-8e54-48fe-b728-bf67c591b978` (2026-05-22 19:10 KST).

**검증 일자**: 2026-05-22
**검증자**: 자동 게이트 (TestFlight 실기기는 업로드 후 사용자)
**트리거**: `548d6c6 fix(ds2): Apple Health Favorites 카드 정확 spec 적용 — material 폐기`
**Root cause**: build 82 Liquid Glass material 접근이 Apple Health 실 카드와 시각적으로 어긋남. 외부 리서치(Apple HIG + sarunw Dark Color Cheat Sheet + WWDC25 Session 323) 결과 solid bg + smaller radius + larger value 가 정확 스펙.

## 변경 범위

| 파일 | 변경 |
|---|---|
| `HealthStyleFavoriteCard.swift` | regularMaterial → secondarySystemGroupedBackground (solid), radius 18→12, padding 14→16, value 22pt.bold→28pt.semibold(rounded), icon 11→13, title caption→footnote.semibold, supporting secondary→tertiary, minHeight 92→84, shadow + overlay stroke 제거 |
| `DashboardView+Shortcuts.swift` | 섹션 헤더 headline → title3.bold, "전체 보기" footnote.semibold |
| `DashboardView.swift` | V2 preview 시 ScrollView 배경 systemGroupedBackground, scrollContentBackground hidden |

## 게이팅

`FeatureFlags.designSystemV2Preview` 컴파일 타임 플래그. 일반 사용자 노출 영향 0 — 디자인 dogfooding cohort 한정.

## 자동 게이트 결과

- ✅ `make plan-verify` PASS (74 파일, 7 심볼)
- ✅ `make verify` ALL CHECKS PASSED (build + lint + arch R1=0 R2=0 R3=0 + 371 unit tests + design-verify)
- ✅ `make ui-test` PASS (PregnancyFlowTests 18)
- ✅ `make smoke-test` PASS (cold launch 10s, crash 0, screenshot `smoke_20260522_190512.png`)

## 3-Agent QA

### 1. Visual / UX
- [x] solid bg + 12pt radius + 16pt padding = Apple Health 실 카드 비례 1:1
- [x] 28pt rounded semibold value = 대형 메트릭 가독성 우선
- [x] tint 적용 (수유/수면/기저귀 시맨틱 컬러)
- [ ] 라이트/다크 모드 전환 시 systemGroupedBackground 자연스러움 (TestFlight 실기기)
- [ ] iPhone SE / 기본 / Pro Max 레이아웃 (TestFlight 실기기)

### 2. Code Quality
- [x] `make verify` ALL CHECKS PASSED
- [x] arch-test Rule 3 baseline = 0 유지
- [x] SwiftLint 0 error (warning 9건은 pre-existing — Badge.swift 라인 길이 + ActivityVM 함수 길이)

### 3. Mobile Responsive
- [x] minHeight 84pt — Dynamic Type accessibility 사이즈에서도 chevron clip 없음 (lineLimit 1 + minimumScaleFactor 0.7)
- [ ] 회전 대응 — 영향 없음 (HStack flex)
- [x] Safe area / 홈 인디케이터 침범 없음 (scroll content)

## 신규 기능 시나리오 검증

- N/A (visual refinement only — 기능 추가 0)

## 결과

**PASS**

(자동 게이트 통과. TestFlight 빌드 83 실기기에서 라이트/다크 모드 + 폼팩터 visual 비교는 사용자 dogfooding 단계.)
