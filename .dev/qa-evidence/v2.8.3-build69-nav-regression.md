# QA Evidence — v2.8.3 빌드 69 (nested NavigationStack fix)

**검증 일자**: 2026-05-17~
**검증자**: 사용자 (TestFlight 실기기)
**트리거**: PR #9 `d31cd06` — nested NavigationStack 8개 view 일괄 제거
**Root cause**: NavigationLink로 push되는 view가 body root에 NavigationStack 재중첩 → iOS 17/18 toolbar 결합 시 latent crash hotspot
**보고된 회귀**: 사용자 "통계 누르면 앱 종료" (StatsView)

## 변경 범위 (PR #9, 8 push-only views + 1 dual-use)

| # | View | 패턴 | 변경 |
|---|---|---|---|
| 1 | `StatsView` | push-only | body root NavigationStack 제거 |
| 2 | `AIAdviceView` | push-only | body root NavigationStack 제거 |
| 3 | `CryAnalysisView` | push-only | body root NavigationStack 제거 |
| 4 | `DiaryView` | push-only | body root NavigationStack 제거 |
| 5 | `GrowthView` | push-only | body root NavigationStack 제거 |
| 6 | `SoundPlayerView` | push-only | body root NavigationStack 제거 |
| 7 | `TodoView` | push-only | body root NavigationStack 제거 |
| 8 | `DashboardPregnancyView` | dual-use | body root NavigationStack 제거 + `DashboardView.swift:52` 에서 wrap |

## 자동 게이트 결과 (이미 통과)

- ✅ make verify (lint/arch/test/design) ALL CHECKS PASSED
- ✅ 단위 테스트 370/370 PASS, 0 skip (PR #7/#8 인프라 + 부채 fix 후 통과)
- ✅ make smoke-test PASS (시뮬레이터 cold launch + 30s 사용 + crash 0)
- ✅ make deploy chain PASS → TestFlight 빌드 69 (`c040f15f-...`, 2026-05-17 업로드)

## 사용자 무회귀 체크리스트 (TestFlight 빌드 69 실기기 필수)

> **목표**: PR #9에서 수정한 8개 view의 **모든 entry point** 진입/이탈 시 crash 없음을 1회씩 확인. 8개 view × 다중 entry point = **총 13개 navigation 경로**.

### 1. StatsView (통계) — 보고된 버그 검증 핵심 ⚠️

- [ ] **설정 → 통계** (SettingsView:113) ✨ **사용자 원 보고 경로 — 반드시 확인**
- [ ] **홈 → "통계 보기" 단축버튼** (DashboardView+Shortcuts:144)
- 각 경로에서: 진입 → 차트 렌더 → 좌상단 back → crash 0

### 2. AIAdviceView (AI 조언)

- [ ] **설정 → AI 조언** (SettingsView:189)
- [ ] **홈 → 헤더 AI 조언** (DashboardView+Header:189)
- 각 경로에서: 진입 → 조언 카드 표시 → back → crash 0

### 3. CryAnalysisView (울음 분석)

- [ ] **건강 → 울음 분석** (HealthView:149)
- 진입 → 5초 녹음 가능 상태 → back → crash 0

### 4. DiaryView (일기)

- [ ] **건강 → 일기** (HealthView:165)
- 진입 → 일기 목록 + 갤러리 탭 전환 → back → crash 0

### 5. GrowthView (성장)

- [ ] **건강 → 성장** (HealthView:88)
- 진입 → WHO 차트 렌더 → 기록 추가 → back → crash 0

### 6. SoundPlayerView (사운드 플레이어) — 4 entry point

- [ ] **설정 → 사운드** (SettingsView:119)
- [ ] **건강 → 사운드** (HealthView:133)
- [ ] **홈 → 헤더 사운드** (DashboardView+Header:276)
- [ ] **FloatingMiniPlayer 탭 → 풀스크린** (FloatingMiniPlayer:73)
- 각 경로에서: 진입 → 재생 → back → 미니 플레이어 잔존 확인 → crash 0

### 7. TodoView (할 일)

- [ ] **설정 → 할 일** (SettingsView:89)
- 진입 → 항목 추가 → 완료 토글 → back → crash 0

### 8. DashboardPregnancyView (임신 대시보드) — dual-use ⚠️

> ⚠️ 현재 RC `pregnancy_mode_enabled=true` + `pregnancy_rollout_pct=0` 상태에서 일반 사용자는 노출 안 됨. **임신 모드 활성화 cohort만 검증 가능**. 사용자 본인 계정이 cohort 진입 안 되면 SKIP 가능.

- [ ] **AppContext=.pregnancyOnly** 시 탭 root 진입 (DashboardView.swift:52, NavigationStack wrap)
- [ ] **홈 → DashboardPregnancyHomeCard 탭 → push** (.both 상태)
- 각 경로에서: 진입 → D-day/주차 카드 렌더 → back → crash 0

## 추가 무회귀 권장 (관련 latent risk)

PR #9는 grep audit으로 명시된 8개만 수정. 다른 NavigationLink push 대상도 동일 패턴 잠재 가능성 — **시간 여유 시 표본 확인**:

- [ ] **건강 → 예방접종 / 병원 리포트** push 진입
- [ ] **설정 → 카탈로그 / 공지사항** push 진입
- [ ] **홈 → Highlight DetailSheet** (sheet, push 아님 — 별 위험 낮음)

## 검증 결과 기록

검증 완료 후 본 파일 하단에 결과 append:

```markdown
## 검증 완료 — 2026-05-XX

- 경로 13/13 PASS, crash 0 → **v2.8.3 App Store 제출 권고**
- 또는 발견된 회귀: <경로> <증상> <빈도>
```

## 다음 단계 (검증 완료 후 분기)

### 무회귀 0건 시
1. H-3 AI 의료 감수 25 샘플 검토 (별도 user action) → 통과 후
2. v2.8.3 App Store 제출 (ASC API 자동화, 빌드 69 사용)
3. Firebase Console RC `highlight_enabled=true` + `highlight_ticker_pct` 5% 단계 활성화

### 회귀 발견 시
1. 회귀 view 식별 → root cause 분석 (nested NavigationStack 외 다른 원인 가능성)
2. hotfix PR → make deploy → 빌드 70+
3. v2.8.3 App Store 제출 보류

## 참조

- PR #9 commit: `d31cd06`
- swift-conventions 룰 추가: `.claude/rules/swift-conventions.md` (NavigationLink push view는 body root NavigationStack 금지)
- 이전 회귀 분석: 사용자 "이전부터 있던 문제" 진술 + StatsView 2026-03-02 MVP부터 NavigationStack 내장 → 시간선 일치 확인
