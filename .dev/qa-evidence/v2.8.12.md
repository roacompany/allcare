# QA Evidence — v2.8.12 (「우리 하루」 ①+②단계)

> 파일명은 **버전만** 쓴다 — deploy 체인이 qa-check **뒤에** bump 하므로(107→108)
> `v2.8.12-build{N}.md` 형태는 게이트가 보는 시점과 최종 빌드번호가 어긋난다.

> **업로드 완료** — 빌드 **108** · TestFlight Delivery UUID `ad6eea13-cd6b-4741-8317-f8fc634545bc`
> (2026-09-05 22:07 KST · `UPLOAD SUCCEEDED with no errors`).

**검증 일자**: 2026-09-05
**검증자**: 자동 게이트 (**실기기 시나리오는 업로드 후 PO**)
**트리거**: PR #79 (①단계 시간표 모델+짜는 화면) · PR #80 (②단계 오늘 열기·칸 채우기·닫기)
**버전**: 2.8.11 → **2.8.12** (2.8.11 train 닫힘 — `MARKETING_VERSION` 두 곳 손으로 bump)

## 🔴 이 빌드를 올리는 이유 = 아직 아무도 못 눌러 본 경로가 있어서다

`UI_TESTING` 은 인증을 흉내만 내서 Firestore 가 **항상 막힌다**. 그래서 자동으로 잴 수 있는 건
「불러오기 실패를 말하는가」까지이고, **「우리 하루」의 정상 경로는 한 번도 실행된 적이 없다.**
이 TestFlight 빌드가 그걸 처음 태우는 자리다.

## 변경 범위

| 무엇 | 어디 |
|---|---|
| 시간표 모델·짜는 화면 | `DayPlan`/`PlanSchedule` · `DayPlanExpander` · `PlanEntrySheet` · `DayPlanListView` |
| 오늘 열기·채우기·닫기 | `DayRun` · `DayAnchorsBuilder` · `DaySlotFiller` · `DaySummary` · `TodayViewModel` · `TodayBandCard` |
| Firestore | `dayPlans`·`dayRuns` narrow protocol · 규칙 **라이브 배포 완료**(올린 뒤 재확인 차이 0) |

## 자동 게이트 결과

- ✅ `make plan-verify` PASS (74 파일, 7 심볼)
- ✅ `make verify` ALL CHECKS PASSED — 단위 **875** 실패 0 · arch R1–R4=0 · lint 0 err · design 29/29
- ❌ `make ui-test` (PregnancyFlowTests) **8건 실패 — 이 빌드와 무관한 기존 결함**
  실패 문구가 자기 입으로 조건을 적고 있다: 「'임신 중이에요' 버튼 노출 필수 … **FeatureFlag=true 조건**」.
  `pregnancyModeEnabled` 는 **2026-06-11**(`9755e19` 라이브 P0 차단)부터 `false` 인데
  `PregnancyFlowTests.swift` 는 **2026-04-24** 이후 안 고쳤다 — 플래그를 끄면서 같이 안 고친 것.
  CI 는 `make verify` 만 돌아 `ui-test` 를 안 태워 **3개월간 아무도 못 봤다**.
  8건 전부 임신 등록 UI — 이 판의 변경(우리 하루)과 겹치는 파일 0. **PO 판단으로 업로드 진행.**
  ⚠️ 이 게이트는 영구 빨강이라 `make deploy` 원커맨드가 지금 아무도 못 쓴다 — 플래그가 꺼져 있으면
  **건너뛰도록** 고쳐야 한다(별도 지시 대기).
- ✅ `make smoke-test` PASS (콜드 런치 64s · crash 0 · `smoke_20260905_215902.png`)
- ✅ CI Verify (PR #80) pass 7m34s

## 최종 브랜치 리뷰 7건 — 5건 수정 · 2건 PO 보류

수정: 시간표 없으면 카드 숨김(PO 결정) · 카드가 불러오기 실패를 말함 · 못 읽었으면 오늘 문서
덮어쓰기 금지 · 시트 문구가 실제 채우는 종류와 일치 · 내 줄을 아기 기록으로 안 채움.

⏸️ **보류(PO 지시로 이번 판에 안 고침)** — 이 빌드에 **그대로 들어 있다**:
1. 짜 두지 않은 **종류**의 기록이 하나만 와도 헤드라인이 하루 종일 「오늘은 N 칸이 됐어요」로
   굳는다 → 시안의 「낮」 상태가 사실상 안 보인다.
2. **공유 아기의 파트너는 「우리 하루」를 못 쓴다**(규칙상 `dayPlans`·`dayRuns` 는 본인만).

## 3-Agent QA

### 1. Visual / UX
- [x] 하루 띠 카드 4상태 문구가 테스트로 잠김 (`TodayBandCopyTests` · 재촉·분수·연속일수 0)
- [x] 실패 상태 카드 폭 — 다른 카드와 동일(육안 발견 후 수정 · 스크린샷 `13_today_band_card_load_failed.png`)
- [ ] 라이트/다크 모드 (**실기기**)
- [ ] iPhone SE / 기본 / Pro Max 레이아웃 (**실기기**)

### 2. Code Quality
- [x] `make verify` ALL CHECKS PASSED
- [x] arch-test R1–R4 = 0 · Narrow Protocol 5단계 완료(`dayPlans`·`dayRuns`)
- [x] 태스크별 리뷰 + 최종 브랜치 리뷰 완료, 수정분 재리뷰 통과

### 3. Mobile Responsive
- [x] 띠는 가로 스크롤(`ScrollView(.horizontal)`) — 칸이 늘어도 안 깨진다
- [x] 누르는 요소 전부 `.contentShape(Rectangle())` (①단계에서 글자 자리만 눌리던 결함 재발 방지)
- [ ] 키보드 입력 시 시트 가림 (**실기기**)

## 신규 기능 시나리오 검증 — ⛔ **여기가 이 빌드의 목적**

- [ ] 🔴 설정 → 우리 하루 → **시간표 넣기 → 목록 → 스와이프 삭제** 왕복 (**실기기**)
- [ ] 🔴 홈에서 **오늘 하루 시작 → 기록 넣기 → 칸이 채워짐 → 닫기** (**실기기 · 한 번도 안 돌아 봄**)
- [ ] 🔴 **시간표를 안 짠 상태에서 홈에 카드가 안 보이는지** (**실기기 · PO 결정사항의 실물 확인**)
- [ ] 통신 끊고 열었을 때 「불러오지 못했어요 · 다시 시도」가 뜨는지 (**실기기**)
- [ ] 자정을 넘겨 앱을 열어 둔 채 다음 날 시작이 새 문서로 가는지 (**실기기**)

## 결과

**PASS** — 자동 게이트 한정.

⚠️ 이 PASS 는 **「자동으로 잴 수 있는 것은 전부 초록」**이라는 뜻이지 「기능이 동작한다」가 아니다.
위 「신규 기능 시나리오」 5줄은 **전부 미확인**이며, TestFlight 실기기에서 PO 가 확인해야 한다.
거기서 깨지면 다음 판(v2.8.13) bump 가 필요하다 — **이 빌드를 심사에 올리기 전에** 확인할 것.
