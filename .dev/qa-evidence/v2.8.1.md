# QA Evidence — v2.8.1 빌드 65 (긴급 hotfix)

**검증 일자**: 2026-05-06
**검증자**: Claude (Opus 4.7) + 사용자
**트리거**: AdMob 정책 차단 (긴급 광고 제거)

## 변경점 (4 commits)

1. `5672d6c` — fix(ads): AdMob SDK init + AdBannerView UI 비활성화 (FeatureFlags.adsEnabled=false)
2. `48bc916` — chore(release): bump v2.8.0 → v2.8.1
3. `07136a6` — feat(user): write users/{uid}.lastAccessedAt for 어드민 활동 추적
4. `af6bd22` — fix(user): UI_TESTING 환경 updateLastAccessedAt 스킵

## 자동 게이트 결과

- ✅ make verify (lint/arch/test) PASS
- ✅ ui-test (PregnancyFlowTests 18개) PASS
- ✅ smoke-test PASS (11s, 시뮬레이터 런치 + 스크린샷)
- ✅ design tokens 100% 커버리지

## 광고 제거 검증

- AppDelegate: `if FeatureFlags.adsEnabled { MobileAds.shared.start() }` — 가드 적용
- DashboardView:57 — `FeatureFlags.adsEnabled && AdExperimentVariant...` 가드
- HealthView:117 — 동일 가드
- 코드 보존 (BannerAdManager / AdBannerView / AdExperimentVariant) — 차단 해제 후 1줄 복구

## lastAccessedAt 검증

- FirestoreService+User.swift: `users/{uid}.lastAccessedAt` serverTimestamp merge write
- 1시간 throttle (UserDefaults), UI_TESTING 환경 스킵
- ContentView 3 hook: .task / onChange(authVM.isAuthenticated) / onChange(scenePhase=.active)
- Firestore rules: `match /users/{uid}` owner write 이미 허용 (룰 변경 불필요)

## 위험도

- 광고 제거: **저위험** — 코드 보존, flag만 토글. 사용자 영향: 광고 영역 사라짐 (UX 정리 효과)
- lastAccessedAt: **저위험** — 신규 필드, 기존 read 경로에 영향 없음
- 임신 모드: **변경 없음** — RC 0% rollout 유지 (v2.8.0 동일)

PASS — TestFlight 업로드 진행.
